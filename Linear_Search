#include <stdio.h>
int main(){
	int arr[100],key,flag=0,n;
	printf("Enter size: ");
	scanf("%d",&n);
	printf("Enter array: ");
	for(i=0;i<n;i++){
		scanf("%d",&arr[i]);
	}
	printf("Enter key: ");
	scanf("%d",&key);
	for(i=0;i<n;i++){
		if(a[i]==key){
			pos=i;
			flag=1;
			break;
		}
	}
	if(flag==1){
		printf("found at position %d\n",pos);
	}
	else{
		printf("%d not found\n",key);
	}
}
